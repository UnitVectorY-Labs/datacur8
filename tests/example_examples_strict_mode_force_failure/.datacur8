version: "0.0.0"
strict_mode: FORCE
types:
  - name: item
    input: json
    match:
      include:
        - "^data/.*\\.json$"
    schema:
      type: object
      required: ["id"]
      properties:
        id: { type: string }
      additionalProperties: true
