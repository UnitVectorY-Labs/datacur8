version: "0.0.0"
types:
  - name: thing
    input: json
    match:
      include:
        - "^data/.*\\.json$"
    schema:
      type: object
      required: ["id"]
      properties:
        id: { type: string }
      additionalProperties: false
